version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd web-application
        - pwd
        - npm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: ./web-application/dist/spa
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
